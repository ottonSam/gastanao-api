spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gastanao
    username: postgress
    password: postgress
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create

api:
  security:
    token:
      secret: my-secret-key